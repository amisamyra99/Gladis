

import 'package:flutter/material.dart';

class sync_calendar_screen extends StatelessWidget {
  const sync_calendar_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
